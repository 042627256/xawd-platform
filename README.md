# XAWD V2 — UX READY

Modern responsive React + TypeScript foundation.

Run:
npm install
npm run dev

Build:
npm run build

The previous V1 package remains the architecture/zero-cost blueprint. This V2 package replaces the static UI shell with a dynamic responsive application experience.