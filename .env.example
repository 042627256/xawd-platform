# Frontend-safe public configuration only.
# Never put API provider keys, private keys, seed phrases, signing keys,
# database credentials, OAuth client secrets, or CEX withdrawal credentials here.
VITE_API_BASE_URL=
VITE_APP_ENV=development
